{
  "api_url": "https://your-dashboard-server.example.com",
  "api_key": "your-secret-api-key-here",
  "timeout": 30,
  "cluster_name": "cluster01",
  "collection_window_days": 7,
  "comments": {
    "api_url": "The URL of your dashboard backend API (without trailing slash)",
    "api_key": "API key for authentication - must match backend configuration",
    "timeout": "HTTP request timeout in seconds (optional, default: 30)",
    "cluster_name": "Override cluster name (optional, auto-detected from SLURM if not provided)",
    "collection_window_days": "Number of days to collect data for (optional, default: 7)"
  }
}
