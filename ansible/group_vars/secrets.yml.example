---
# Encrypted Secrets File (Example)
#
# Create your actual secrets file with:
#   ansible-vault create group_vars/secrets.yml
#
# Then copy this content and fill in your actual values

# API Keys for data ingestion (comma-separated)
# Generate with: openssl rand -base64 32
api_keys: "REPLACE-WITH-ACTUAL-KEY-1,REPLACE-WITH-ACTUAL-KEY-2"

# Secret key for JWT session tokens
# Generate with: openssl rand -hex 32
secret_key: "REPLACE-WITH-ACTUAL-SECRET-KEY-MINIMUM-32-CHARS"

# SAML IdP X.509 Certificate
# Get this from your IdP metadata or IT administrator
# Paste the certificate content (can be multi-line)
saml_idp_cert: |
  MIIDXTCCAkWgAwIBAgIJAKHd...
  REPLACE WITH YOUR ACTUAL IDP CERTIFICATE
  ...

# Optional: If you need other sensitive values
# database_password: "db-password"
# smtp_password: "email-password"
